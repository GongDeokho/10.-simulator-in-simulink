-개발 목표
EVCS 내에서 EV가 들어왔을 때, EV 파라미터들이 생성되고(trigger)
V2G 지령을 보내고 그 과정에서 불확실성이 적용된다.

시작할 때 설정된 값이 EVSE 개수.

EVSE 상태에 따라 EV behavior 모델을 확률분포를 통해 생성을 한다.

but, EVSE 개수에 딱 맞춰서 생성되진 않는다.

ex. EVSE 10대면,

처음에는 EVSE 10대 모두 운용 가능이기 때문에.

EV behavior 생성이 trigger 된다. (EV parameter 0~10대 랜덤하게 생성)

차량 index라는게 없어지는거랑 같은 말
queing 삭제.


필요 모듈 --------------------------------------------

parameter 설정하는 모듈,

----Loop

불확실성이랑 ev behavior 생성하는 확률분포 모듈
--> 세분화 (정규, 이항 x, 동작별)


데이터 업데이트하는 모듈

스케줄링 모듈 --> 더미 데이터 발생용. (간단한 매트랩 모듈)

-----
결과 정리하는 모듈

세부적 x, 모듈간 배치만.
outline --> 9일까지.
모듈별로 진행.